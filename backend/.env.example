# 이 파일을 복사해서 .env로 만들고 실제 값을 채워 넣으세요
# .env 파일 자체는 절대 git에 올리지 마세요 (.gitignore에 이미 포함되어 있음)

PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=real_password
DB_NAME=YB-bly

JWT_SECRET=아무도_모르는_긴_랜덤_문자열
JWT_EXPIRES_IN=1h

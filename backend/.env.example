# 복사해서 .env로 만들고 값을 채우세요. (.env 자체는 git에 올라가지 않습니다)

PORT=3000

# SQLite는 파일 하나가 DB라서 경로만 지정하면 됩니다. (서버 설치 필요 없음)
DB_FILE=./data/shop.db

JWT_SECRET=긴_랜덤_문자열
JWT_EXPIRES_IN=30m

# 정상 버전으로 실행할지, 의도한 취약점이 포함된 버전으로 실행할지 스위치
# true = 취약 버전 라우트 사용 (모의해킹 대상 배포용), false = 정상(보안 적용) 버전
VULN_MODE=false
